<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.bgg">

    <uses-permission android:name="android.permission.INTERNET" />

    <application

        android:name=".BGGApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_bgg_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_bgg_icon_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".ModifyUpdatesActivity" />
        <activity android:name=".FavouriteActivity" />
        <activity android:name=".AddToCustomListActivity" />
        <activity android:name=".CustomListActivity" />
        <activity android:name=".ListManagingActivity" />
        <activity android:name=".GameDetailsActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".GameListActivity" />
        <activity android:name=".ListFavouriteGamesActivity" />
    </application>

</manifest>